{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Publish http to gh-pages branch",
            "type": "shell",
            "command": "git subtree push --prefix http origin gh-pages",
            "problemMatcher": []
        }
    ]
}
